for file in $(find . -type f -name "*.exe"); do
    # Получаем новое имя файла, заменяя расширение .exe на .sh
    new_file="${file%.exe}.sh"

    # Переименовываем файл
    mv "$file" "$new_file"

    echo "Файл $file переименован в $new_file"
done
